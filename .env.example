# Database
DATABASE_URL="postgresql://eafc26:eafc26@localhost:5432/eafc26"

# API Configuration
PORT=3001
NODE_ENV=development

# Frontend URL (for CORS)
FRONTEND_URL=http://localhost:3000

# SBC Solver URL
SBC_SOLVER_URL=http://localhost:8000

# EA FC Companion App Credentials (add your credentials here)
EA_FC_EMAIL=
EA_FC_PASSWORD=

# Selenium Configuration
SELENIUM_BROWSER=chrome
SELENIUM_HEADLESS=false
SELENIUM_TIMEOUT=10000

# Chrome Profile for Session Persistence (optional)
# Use a dedicated directory to save login sessions
# Example: ./selenium-profile (recommended, already in .gitignore)
SELENIUM_CHROME_PROFILE_DIR=./selenium-profile

# Persistent Chrome Configuration
# The server will automatically detect and connect to Chrome running on port 9222
# To start persistent Chrome: ./tools/scripts/start-persistent-chrome.sh
# To stop persistent Chrome: ./tools/scripts/stop-persistent-chrome.sh
# Chrome will persist across server restarts (won't close when server stops)
#
# Optional: Explicitly set debugger address (auto-detected if running on port 9222)
SELENIUM_DEBUGGER_ADDRESS=

# Next.js Public Variables
NEXT_PUBLIC_API_URL=http://localhost:3001
